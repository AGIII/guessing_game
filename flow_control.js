// Basic flow control
// IF / ELSE IF / ELSE
// WHILE
// DO WHILE

if (condition) {
  // Do some code
} else if (condition2) {
  // Do some other code
} else if (condition3) {
  // Do some code
} else {
  // Do some other code
}

while (condition) {
  //do some code
}
